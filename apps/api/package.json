{
  "name": "@raltic/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "dependencies": {
    "@raltic/agent": "workspace:*",
    "@raltic/auth-core": "workspace:*",
    "@raltic/chat-room": "workspace:*",
    "@raltic/db": "workspace:*",
    "@raltic/protocol": "workspace:*",
    "@raltic/sandbox-container": "workspace:*",
    "@sentry/cloudflare": "^10.53.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.13",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.21",
    "@cloudflare/workers-types": "^4.20250108.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0"
  }
}
